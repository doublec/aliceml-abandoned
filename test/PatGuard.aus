(* guarded patterns and differently ordered records *)

let
    fun g _ = false
in
    fn x =>
    case x of
	{1 = x when (g x), 2 = (y, _) when (g y)} => 100
      | {2 = (y, _) when (g y), 1 = x when (g x)} => 200
end
