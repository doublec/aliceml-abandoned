(*
 * Author:
 *   Leif Kornstaedt <kornstae@ps.uni-sb.de>
 *
 * Copyright:
 *   Leif Kornstaedt, 2001
 *
 * Last change:
 *   $Date$ by $Author$
 *   $Revision$
 *)

import
    structure MkBatchCompiler
from "MkBatchCompiler"

import
    structure SMLToComPlusRecursiveCompiler
from "SMLToComPlusRecursiveCompiler"

structure SMLToComPlusBatchCompiler =
    MkBatchCompiler(structure RecursiveCompiler =
			SMLToComPlusRecursiveCompiler
		    val executableHeader = "")
