import structure Space       from "x-alice:/lib/gecode/Space"
import signature TREENODE    from "TREENODE-sig"
import functor   BasicPolicy from "Policy"
import structure MkTreeNode  from "MkTreeNode"

local
signature ARG = (val root : Space.space val rdist : int)
in
functor TreeNode (Arg:ARG) :> TREENODE = MkTreeNode (BasicPolicy Arg)
end
