(*
 * Author:
 *   Leif Kornstaedt <kornstae@ps.uni-sb.de>
 *
 * Copyright:
 *   Leif Kornstaedt, 2001
 *
 * Last Change:
 *   $Date$ by $Author$
 *   $Revision$
 *)

signature UNSAFE_REMOTE_COMPONENT =
    sig
	structure UnsafeRemote:
	    sig
		exception Ticket

		val proxy: ('a -> 'b) -> ('a -> 'b)
		val offer: Package.package -> string
		val take: string -> Package.package        (* Ticket *)
	    end
    end
