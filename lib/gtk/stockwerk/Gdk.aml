
import structure GtkUtils from "GtkUtils"
import structure GdkCore from "GdkCore"
import structure GdkEnums from "GdkEnums"
import signature GDK from "GDK-sig"

structure Gdk :> GDK =
struct
    val _ = GtkUtils.init()
    open GdkCore
    open GdkEnums 
end
