signature GTK_TYPES =
    sig
	type object
	exception TypeMismatch of string
	datatype arg = 
	    BOOL of bool
	  | INT of int
	  | REAL of real
	  | STRING of string
	  | OBJECT of object
	  | LIST of object list
    end
