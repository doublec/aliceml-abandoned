import structure GtkCore from "GtkCore"
import structure GdkEnums from "GdkEnums"
import structure PangoEnums from "PangoEnums"

signature GNOME_CANVAS =
sig

    type object = GtkCore.object

<<< "GTK_CORE-sig.aml" >>>
<<< "GNOME_CANVAS_ENUMS-sig.aml" >>>
<<< "GNOME_CANVAS_UNSAFE-sig.aml" >>>

    val itemCreate : object * int * (string * arg) list -> object
    val makePoints : int list -> object

end
