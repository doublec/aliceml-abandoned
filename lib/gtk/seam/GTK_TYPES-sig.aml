signature GTK_TYPES =
    sig
(**)	type object
(**)	exception TypeMismatch of string


	datatype event = UNSUPPORTED of object

	datatype arg = 
	    BOOL of bool
	  | INT of int
	  | REAL of real
	  | STRING of string
	  | OBJECT of object
	  | LIST of object list
	  | EVENT of event
    end
