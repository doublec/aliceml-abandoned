(*
 * Author:
 *   Guido Tack <tack@ps.uni-sb.de>
 *
 * Copyright:
 *   Guido Tack, 2002
 * 
 * Last change:
 *   $Date$ by $Author$
 *   $Revision$
 *
 *) 

import structure UnsafeMinimizer from "UnsafeMinimizer"

structure Minimizer =
  struct
    fun minimize a = let
        val b = UnsafeMinimizer.newPartition()
        val _ = UnsafeMinimizer.loadGraph(b, a)
    in
        UnsafeMinimizer.minimize b
    end
  end
