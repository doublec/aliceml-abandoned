#ifndef __commonop_hh__
#define __commonop_hh__

#if defined(INTERFACE)
#pragma interface
#endif

class CommonOp {
public:
  static word Sync(word a) {
    return a; // to be determined
  }
};

#endif
